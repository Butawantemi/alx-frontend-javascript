// An Array.
const city = ['Dodoma', 'Dar es Salaam'];

// Add multiple elements to the beginning of an array.
const newLength = city.unshift('Mwanza', 'Arusha');

// Output
console.log(city);
console.log(newLength);
