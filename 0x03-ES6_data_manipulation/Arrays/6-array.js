// An Array
const city = ['Dodoma', 'Dar es Salaam', 'Mwanza', 'Arusha'];
const start = -2;

// Remove maltiple elements from the end of an array.
const removedElements = city.splice(start);

// output the array and the removed elements.
console.log(city);
console.log(removedElements);
