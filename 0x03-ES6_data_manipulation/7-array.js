// An Array
const city = ['Dodoma', 'Dar es Salaam', 'Mwanza'];

// Add an element to the end of an array.
const removedElement = city.shift();

// Output the array and its length.
console.log(city);
console.log(removedElement);
