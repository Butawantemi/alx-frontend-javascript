// An Array
const city = ['Dodoma', 'Dar es Salaam', 'Mwanza'];

// Remove the last element from an array.
const lastElement = city.pop();

// Output the array and the removed element.
console.log(city);
console.log(lastElement);
