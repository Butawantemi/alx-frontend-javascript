const fruit = ['manzana', 'banana', 'pera', 'uva', 'sandia'];

// console.log(Object.keys(fruit));

// loop through the array and print all the elements
fruit.forEach((item, index) => {
    console.log(`Index: ${index} - Item: ${item}`);
});